---
description: Performance profile — find bottlenecks, memory leaks, slow queries
agent: build
subtask: true
---

Use the performance-profile skill:
1. Container stats — CPU, memory per service
2. N+1 query detection — grep for queries in loops
3. Bundle analysis — check for large imports
4. API response times — curl endpoints

Report: bottlenecks with file:line, impact estimate, and optimization suggestions.
